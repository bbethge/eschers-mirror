#!/usr/bin/env python

import clutter
from Config import config
from Stage import Stage

if __name__ == '__main__':
	stage = Stage()
	stage.show()
	clutter.main()
	config.write()

# vim: set ts=4 sts=4 sw=4 ai noet :
